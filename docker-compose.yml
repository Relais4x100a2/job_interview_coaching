services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      FLASK_DEBUG: "1"
      UV_NO_DEV: "1"
      UV_LINK_MODE: copy
    volumes:
      - .:/app
      - venv:/app/.venv
    restart: unless-stopped

volumes:
  venv:
